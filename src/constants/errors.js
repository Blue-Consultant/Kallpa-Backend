const EXISTS = Object.freeze({
  EXISTS_USER: 'EXISTS_USER',
});

const ERRORS = Object.freeze({
  ERROR_TO_SAVE_USER: 'ERROR_TO_SAVE_USER',
});

const EMPTYS = Object.freeze({
  EMPTY: 'EMPTY',
});

const DONT_MATCH = Object.freeze({
  DONT_MATCH_PASSWORD: 'DONT_MATCH_PASSWORD',
});

const DONT_EXISTS = Object.freeze({
  DONT_EXISTS_USER: 'DONT_EXISTS_USER',
  DONT_EXISTS_POST: 'DONT_EXISTS_POST',
});

module.exports = {
  EXISTS,
  ERRORS,
  EMPTYS,
  DONT_MATCH,
  DONT_EXISTS,
};
